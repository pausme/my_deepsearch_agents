# DeepSearch Agents 环境变量示例
# 复制为 .env 后按本机实际服务地址和密钥调整，不要提交真实密钥

# LLM 配置
OPENAI_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
OPENAI_API_KEY=你的大模型_API_KEY
LLM_QWEN_MAX=qwen-max

# Tavily 配置
# 在 https://app.tavily.com/ 注册账号，然后创建API KEY
TAVILY_API_KEY=你的_TAVILY_API_KEY

# RAGFlow 配置
RAGFLOW_API_URL=http://your-ragflow-host
RAGFLOW_API_KEY=ragflow-your-api-key

# MySQL 配置
MYSQL_USER=root
MYSQL_PASSWORD=root
MYSQL_DATABASE=deepsearch_db
MYSQL_HOST=localhost
# docs/docker-compose.yaml 建议映射到 3307，避免和本机已有 MySQL 3306 冲突
MYSQL_PORT=3307
# 与初始化 SQL 和 Docker MySQL 服务保持一致，保证中文数据按 utf8mb4 读写
MYSQL_CHARSET=utf8mb4
MYSQL_COLLATION=utf8mb4_unicode_ci
MYSQL_SQL_MODE=TRADITIONAL

# 服务配置（部署相关，详见 docs/deployment.md）
# 日志级别：DEBUG / INFO / WARNING
LOG_LEVEL=INFO
# 业务元数据库（SQLite）文件路径；默认 app/data/renovation.sqlite3，生产建议移到数据盘
# RENOVATION_DB_PATH=/var/lib/renovation-agent/renovation.sqlite3
# 前端构建产物目录；存在时由 FastAPI 直接托管（单进程部署）
# 设为 disabled 可关闭托管（前后端分离部署 + nginx）
# FRONTEND_DIST=./frontend/dist
# 允许的跨域来源，逗号分隔；生产环境务必改为具体域名，如 https://renov.example.com
ALLOWED_ORIGINS=*
